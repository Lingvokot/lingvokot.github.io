image: node:6

stages:
  - test
  - deploy

cache:
  paths:
    - node_modules/
before_script:
  - source .env
  - npm install

test:
  stage: test
  script:
    - npm run test

pages:
  stage: deploy
  script:
    - npm run build
    - cp -r src public/
    - cp payomb* public/
    - mv dist public/
    - mv index.html public/
    - mv policy.html public/
    - find public/
  artifacts:
    paths:
    - public
  only:
    - master
