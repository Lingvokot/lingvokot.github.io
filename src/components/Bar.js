import React from "react";
import "src/styles/Bar.css";

const Bar = React.createClass({
  render () {
    return (
      <h1 className="Bar">Hello from Bar!</h1>
    );
  }
});


export default Bar;
